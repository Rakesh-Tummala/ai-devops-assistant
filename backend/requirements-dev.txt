-r requirements.txt
pytest==8.3.4
# Pinned <0.28: this project's fastapi==0.109.2 bundles an older Starlette
# whose TestClient still passes httpx.Client(app=...), a kwarg httpx 0.28
# removed entirely.
httpx==0.27.2
mypy==1.14.1
types-requests==2.32.0.20241016
